# Database Settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=node_api_db

# Authentication Settings
JWT_SECRET=nest-secret-replace-me-in-production
JWT_EXPIRES_IN=1h

# Server Settings
PORT=3000
